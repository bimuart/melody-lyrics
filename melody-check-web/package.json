{
  "name": "melody-check-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:core-js": "node scripts/sync-core-js.mjs",
    "sync:demo-cases": "node scripts/sync-demo-cases.mjs",
    "sync:assets": "npm run sync:core-js && npm run sync:demo-cases",
    "predev": "npm run sync:assets",
    "prebuild": "npm run sync:assets",
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.32"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@vitejs/plugin-vue": "^6.0.6",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vue-tsc": "^3.2.7"
  }
}
